# Tasks

## 1. Documentation Only

- [x] 1.1 Sync the delta specs for `user-auth` and `user-management` into the main specs directory, then verify `openspec list --specs` shows both capabilities and `openspec validate` reports no issues
- [x] 1.2 Review the synced main specs against the current auth-service implementation to confirm they reflect actual behavior